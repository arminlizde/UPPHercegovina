<UserControl
    x:Class="UPPHercegovina_Mobile.Controls.Product_MoreInfoTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UPPHercegovina_Mobile.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Loaded="Grid_Loaded"
          Height="82"
          VerticalAlignment="Top">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="2"/>
                <RowDefinition/>
                
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource TileOnTileColor}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Proizvod"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="1"
                           Text="Neto"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="2"
                           Text="Cijena"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="3"
                           Text="Kvalitet"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>


            </Grid>

            <StackPanel Width="250" Grid.Row="1" VerticalAlignment="Bottom" Background="Transparent"/>

            <Grid Grid.Row="2" Background="{StaticResource TileOnTileColor}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>

                </Grid.ColumnDefinitions>


                <TextBlock Grid.Column="0"
                           x:Name="txtProduct"
                           Text="{Binding Product}"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="1"
                           Text="{Binding Neto}"
                           x:Name="txtNeto"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="2"
                           Text="{Binding ValuePerKgString}"
                           x:Name="txtValuePerKg"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>

                <TextBlock Grid.Column="3"
                           Text="{Binding Quality}"
                           x:Name="txtQuality"
                           VerticalAlignment="Center"
                           Margin="5,0,0,0"
                           FontSize="15"/>


            </Grid>
        </Grid>

    </Grid>
</UserControl>
